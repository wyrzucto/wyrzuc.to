<nav class="navbar top-navbar navbar-fixed-top">
  <div class="container-fluid">
    <div id="navbar" class="navbar-collapse collapse">
      <%= form_tag '#', remote: true, class: "navbar-form" do %>
        <div class="form-group">
          <%= label_tag :waste_type, "Gdzie mam wyrzucić", class: "control-label" %>
          <%= text_field_tag :waste_type, nil, class: "form-control input-sm", placeholder: t("placeholders.waste_type") %>
        </div>
      <% end %>
    </div>
  </div>
</nav>

<div id="map-canvas"></div>

<%= render 'left_buttons' %>

<%= render 'right_sidebar' %>
